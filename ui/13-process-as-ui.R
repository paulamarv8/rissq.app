tabItem("process-as",
        tags$head(tags$style(
          HTML('.content-wrapper, .right-side {
                                  background-color: white;
                                }')
        )),
        div(style = "background-color:white;",
            fluidRow(column(
              width = 12,
              align = "left",
              h1("Process aceptance sampling"),
              p("Tools and method to perform an aceptance sampling analysis")
            ))))
