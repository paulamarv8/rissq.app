tabItem(
  "process-as",
  fluidRow(
    column(
      width = 12,
      align="left",
      h1("Process aceptance sampling"),
      p("Tools and method to perform an aceptance sampling analysis")
    )
  )
)
